session: zrp

root: ~/code/zrp

windows:
  # - name: frente
  #   root: ./frente
  - name: ops
    root: ./ops
    commands:
      - export AWS_PROFILE=zrp-prod
  - name: caps
    # root: .
    # layout: main-vertical
    # panes:
    #   - root: .
    #     commands:
    #       - export AWS_PROFILE=zrp
    #       - terraform plan -var-file=vault.tfvars

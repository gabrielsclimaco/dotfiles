session: personal

root: ~/code/personal

windows:
  - name: monitoring
    commands:
      - bashtop
  - name: ricing
    root: ~/.config
    commands:
      - pfetch
  - name: config
    root: ~/ansible-desktop-config

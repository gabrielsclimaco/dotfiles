session: personal

root: ~/code/personal

windows:
  - name: monitoring
    commands:
      - gotop
  - name: ricing
    root: ~/.config
    commands:
      - neofetch
  - name: config
    root: ~/ansible-desktop-config
